## 1. 项目搭建

- [x] 1.1 创建 Android 手机应用和 Wear OS 手表应用的项目结构。
- [x] 1.2 添加共享的标记领域模型，包含标记 ID、触发时间戳、位置元数据、捕获状态、来源设备和同步状态。
- [x] 1.3 为手表端和手机端添加标记事件本地持久化。
- [x] 1.4 添加基础应用导航，覆盖手机每日时间线、地图视图和手表捕获状态。

## 2. 手表标记捕获

- [x] 2.1 实现快捷启动的手表捕获入口，确保每次触发只创建一个标记。
- [x] 2.2 在开始定位捕获前，立即记录标记触发时间戳。
- [x] 2.3 防止手表 Activity 生命周期重建期间重复创建标记。
- [x] 2.4 实现尽力而为的单次定位捕获，包含纬度、经度、可用时的海拔、精度、提供方/来源和捕获状态。
- [x] 2.5 即使定位捕获失败或超时，也持久化标记。
- [x] 2.6 为标记已接受和记录失败状态实现震动反馈。
- [x] 2.7 添加 onboarding 文案或设置指引，说明如何将 Watch7 Home 键双击快捷方式配置为打开手表应用。

## 3. 手表到手机同步

- [x] 3.1 实现标记同步载荷格式，包含稳定标记 ID、时间戳、位置元数据、捕获状态和来源元数据。
- [x] 3.2 实现手表端未同步标记事件的排队同步。
- [x] 3.3 实现手机端标记接收，并按稳定标记 ID 幂等 upsert。
- [x] 3.4 当后续同步载荷包含位置元数据时，更新手机端已有标记记录。
- [x] 3.5 当配对设备传输通道可用时，重试未同步的手表标记。
- [x] 3.6 只有在确认手机端已接收后，才将手表标记标记为已同步。

## 4. 手机时间线

- [x] 4.1 实现按触发时间戳排序的每日标记加载。
- [x] 4.2 从所选日期的相邻标记推导已关闭时间段。
- [x] 4.3 从最新标记到当前时间推导开放式当前时间段。
- [x] 4.4 当接收到晚到标记时，重新计算受影响的时间段。
- [x] 4.5 构建每日时间线 UI，作为手机应用的主要回顾视图。
- [x] 4.6 当标记存在位置元数据时，为时间段展示简洁的位置上下文。

## 5. 时间段标签

- [x] 5.1 添加时间段标签持久化，将标签关联到推导出的时间段边界，而不是标记记录。
- [x] 5.2 实现为时间段分配标签。
- [x] 5.3 实现修改已有时间段标签。
- [x] 5.4 展示未标记时间段，并且不阻塞时间线回顾。
- [x] 5.5 当晚到标记改变时间段边界时，正确保留或失效标签。

## 6. 地图视图

- [x] 6.1 实现所选日期地图视图，展示具有可用坐标的标记点。
- [x] 6.2 在地图中省略没有可用坐标的标记，同时保留它们在时间线中的可见性。
- [x] 6.3 只在连续展示的标记点之间绘制直线段。
- [x] 6.4 避免将标记连接呈现为真实路线还原或连续 GPS 追踪。
- [x] 6.5 支持在查看某个时间段时突出显示对应的标记点和直线连接。

## 7. 验证

- [x] 7.1 验证快捷捕获只创建一个标记，并且在定位完成前记录时间戳。
- [x] 7.2 验证位置不可用时标记捕获仍然会被持久化。
- [x] 7.3 验证重复同步载荷不会创建重复的手机端标记。
- [x] 7.4 验证晚到标记会正确触发时间线时间段重算。
- [x] 7.5 验证时间段标签存储在时间段上，而不是标记记录上。
- [x] 7.6 验证地图点和直线连接符合所选日期的标记顺序。

## 8. 实测反馈修复

- [x] 8.1 修复“手表端总是显示位置不可用”的定位获取问题。
- [x] 8.2 为手表端定位失败或不可用补充可诊断原因，至少区分权限缺失、系统定位关闭、定位超时和定位提供方未返回坐标。
- [x] 8.3 验证手表端定位成功时，同一标记同步到手机后包含纬度和经度。
- [x] 8.4 修正手机端每日时间线排序，确保时间段按开始时间从早到晚、自上而下展示。
- [x] 8.5 验证最新开放式当前时间段展示在当天时间线底部，而不是排在最上方。
- [x] 8.6 调整手表快捷打点语义：标记创建并持久化后即为成功，定位结果只异步更新同一条标记的位置状态。

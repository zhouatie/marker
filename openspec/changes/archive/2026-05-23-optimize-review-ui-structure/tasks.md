## 1. 回顾视图结构

- [x] 1.1 调整 `TimelineScreen` 的 tab 内容分配，使时间线 tab 不再在时间段列表前渲染完整地图画布。
- [x] 1.2 将地图内容限制在地图 tab 中展示，并保持时间线和地图共享同一个所选时间段状态。
- [x] 1.3 确保空状态仍然在没有时间段时展示可操作的开始记录引导。

## 2. 手表快捷入口提示策略

- [x] 2.1 移除已有时间段时主内容顶部常驻的“手表快捷入口”说明卡片。
- [x] 2.2 将 Watch7 Home 键双击配置说明整合到无标记/无时间段空状态中。
- [x] 2.3 保留后续迁移到设置或帮助入口的组件边界，避免把 onboarding 文案硬编码到时间段列表里。

## 3. 时间线视觉与交互

- [x] 3.1 优化时间段行布局，明确区分时间范围、标签、位置上下文和可点击区域。
- [x] 3.2 优化时间段选中状态，使用户能看出当前关注的时间段，并且不影响标签编辑入口。
- [x] 3.3 保持时间段行内的简洁位置上下文展示，不把完整地图语义塞回时间线 tab。

## 4. 地图 tab 视觉与交互

- [x] 4.1 将地图 tab 的画布调整为该 tab 的主要内容，而不是小型预览卡片。
- [x] 4.2 在地图 tab 中展示标记点直线连接，并保留“不代表真实路线”的说明。
- [x] 4.3 当存在所选时间段且坐标可用时，高亮对应标记点或连接线。
- [x] 4.4 当所选时间段缺少坐标时，不绘制误导性高亮，并保持时间线中的选中状态。

## 5. 验证

- [x] 5.1 手动验证有时间段时默认时间线 tab 首屏优先展示回顾内容，而不是手表快捷入口或完整地图。
- [x] 5.2 手动验证切换到地图 tab 后能看到独立地图回顾内容，且 tab 差异不只是隐藏列表。
- [x] 5.3 手动验证选择时间段后在时间线和地图 tab 间切换时选中上下文保持一致。
- [x] 5.4 手动验证无时间段时仍能看到如何通过 Watch7 Home 键双击开始记录的引导。

## 6. 固定顶部区域

- [x] 6.1 将顶部标题、日期和 tab 切换控件移出时间线滚动容器，只让下方时间段列表滚动。

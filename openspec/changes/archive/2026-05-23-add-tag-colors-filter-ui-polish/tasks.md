## 1. 标签颜色基础能力

- [x] 1.1 在手机端 UI 层新增标签颜色模型，包含容器色、文字色和边框/强调色。
- [x] 1.2 为预设标签配置稳定颜色，并为自定义标签实现基于标签文本的稳定调色板映射。
- [x] 1.3 为未标记状态提供中性视觉样式，避免与具体活动标签混淆。

## 2. 标签筛选状态与数据

- [x] 2.1 定义时间线筛选状态，支持全部、未标记和指定标签。
- [x] 2.2 从当前日期时间段中生成筛选项，按全部、未标记、预设标签、自定义标签的顺序展示。
- [x] 2.3 根据当前筛选条件计算时间线列表内容，并保留原有最近优先排序。
- [x] 2.4 在筛选结果为空时提供可恢复到全部的状态和回调。

## 3. 每日回顾 UI 美化

- [x] 3.1 优化顶部标题、日期选择和时间线/地图切换控件，使顶部区域紧凑且在列表滚动时保持可见。
- [x] 3.2 在时间线 tab 增加标签筛选 chip 区，展示全部、未标记和已有标签的颜色与选中态。
- [x] 3.3 优化时间段行布局，明确区分时间范围、进行状态、标签、位置上下文和标记点管理入口。
- [x] 3.4 优化时间段选中态，确保选中反馈清楚且不改变标签颜色含义。
- [x] 3.5 优化普通空状态和筛选空状态，确保二者文案、操作和手表快捷入口引导互不混淆。

## 4. 标签编辑入口

- [x] 4.1 在时间段行的标签 chip 上应用标签颜色，并保留点击编辑标签能力。
- [x] 4.2 在标签编辑弹窗中为预设标签展示对应颜色和明确选中态。
- [x] 4.3 确保自定义标签保存后在时间段行和筛选栏中使用一致颜色。

## 5. 验证

- [x] 5.1 验证默认进入时间线 tab 时展示全部时间段，筛选控件选中“全部”。
- [x] 5.2 验证选择某个标签后只展示该标签时间段，选择“未标记”后只展示未标记时间段。
- [x] 5.3 验证筛选无结果时展示筛选空状态，并可返回全部。
- [x] 5.4 验证切换到地图 tab 后仍能使用当前选中时间段上下文，不破坏已有地图高亮行为。
- [x] 5.5 验证保存标签、修改标签和删除标记后的标签清理语义保持不变。
